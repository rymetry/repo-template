name: Agents Autonomy

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck autonomy engine
        run: pnpm agents:typecheck

      - name: Test autonomy engine
        run: pnpm agents:test

      - name: Dry-run autonomy driver
        run: pnpm agents:drive -- --dry-run
